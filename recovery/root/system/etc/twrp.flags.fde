# mount point   fstype  device                      device2                     flags

# Data
/data           auto    /dev/block/bootdevice/by-name/userdata                  flags=encryptable=footer;fsflags="discard"
